# HTTP Server
server:
  port: 8888

spring:
  # Spring Cloud Config Server Repository
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SI657-2302-WX71-DigitAlholics/SpringCloudConfig-GitRepository

  # Spring properties in case you use different profiles

